import { InlineConfig, mergeConfig } from "vite";
import { Bundler, BundlerOptions } from "@vuepress/core";
import { Options } from "@vitejs/plugin-vue";
//#region src/types.d.ts
/**
 * Options for bundler-vite
 */
interface ViteBundlerOptions extends BundlerOptions {
  /**
   * Vite options
   */
  viteOptions?: InlineConfig;
  /**
   * Options for @vitejs/plugin-vue
   */
  vuePluginOptions?: Options;
  /**
   * Edit the internal Vite config.
   *
   * @param config - Vite config
   * @param isServer - Whether it is server bundle
   * @param isBuild - Whether in build mode
   * @returns if returns a configuration object, it will be used as the new vite config, otherwise the original config object will be used
   */
  configureVite?: (config: InlineConfig, isServer: boolean, isBuild: boolean) => InlineConfig | void;
}
//#endregion
//#region src/viteBundler.d.ts
declare const viteBundler: (options?: ViteBundlerOptions) => Bundler;
//#endregion
export { type ViteBundlerOptions, viteBundler as default, viteBundler, mergeConfig as viteMergeConfig };